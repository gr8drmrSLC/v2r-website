# TODO — v2r-website

- **Create a Cloudflare `CF_CACHE_PURGE_TOKEN` (and note the zone's ID) for v2adv.com's zone.** Founder said "I will do this later" (2026-09-05). Requires v2adv.com to first be a Cloudflare zone at all (see the Turnstile item below — not yet true as of 2026-09-06). The purge call itself is already built and wired up: `.github/workflows/cloudflare-cache-purge.yml` (added 2026-09-06) fires on every push to `master` — the same event GitHub Pages' legacy build already republishes on — and no-ops cleanly (doesn't fail the workflow) until both `CF_ZONE_ID` and `CF_CACHE_PURGE_TOKEN` exist as this repo's GitHub Actions secrets. Once the zone exists: Cloudflare dashboard → the v2adv.com zone → Overview → copy the Zone ID; My Profile → API Tokens → "Cache Purge" template scoped to that zone → copy the token; this repo's Settings → Secrets and variables → Actions → add both. Nothing else to build — the next push to `master` starts purging automatically. Full context: `docs/capabilities/cloudflare-and-twilio-infrastructure-notes.md`'s "Cloudflare Cache — Two Layers, Both Must Be Purged" section in `v2r-enterprise-knowledge`.
- **Generate a Cloudflare Turnstile site key + secret key for v2adv.com** (2026-09-06). Same Cloudflare account already used across the WordPress portfolio; add a new site entry for v2adv.com, "Managed" widget mode, matching ACT Life and Health's real configuration. `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` go into `v2r-platform`'s `.env` (see that repo's `.env.example`) — the client-account registration gate at `/account/register` no-ops without them (registration works, just unprotected by Turnstile) until they're set.
