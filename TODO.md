# TODO — v2r-website

- ~~Create a Cloudflare `CF_CACHE_PURGE_TOKEN` (and note the zone's ID) for v2adv.com's zone~~ **Resolved 2026-09-07.** Both `CF_ZONE_ID` (`27025cc4b25c0ad3f95afd43f5d8331b`) and `CF_CACHE_PURGE_TOKEN` are now set as GitHub Actions secrets on this repo, verified active against Cloudflare's `/user/tokens/verify` endpoint. `.github/workflows/cloudflare-cache-purge.yml` will now actually purge on every push to `master` instead of no-oping. **Correction to the note this replaces:** the dashboard no longer offers a "Cache Purge" *template* under My Profile → API Tokens — that template has been removed. The token had to be built via "Create Custom Token" instead, with Permissions set manually to `Zone` / `Cache Purge` / `Purge`, and Zone Resources scoped to `Include` / `Specific zone` / `v2adv.com`. Anyone repeating this later should expect Custom Token, not the template shortcut. The `?v=N` cache-busting query strings already in `index.html` (`styles.css?v=5`, `globe.js?v=13`, etc.) can stay as-is — they're harmless now, but no longer strictly required for cache invalidation on future pushes. Full context: `docs/capabilities/cloudflare-and-twilio-infrastructure-notes.md`'s "Cloudflare Cache — Two Layers, Both Must Be Purged" section in `v2r-enterprise-knowledge`.
- ~~Generate a Cloudflare Turnstile site key + secret key for v2adv.com~~ **Resolved 2026-09-07.** Created a new "v2adv.com client registration" Turnstile widget in the same Cloudflare account, Managed mode, hostname `v2adv.com`. Both keys are set in `v2r-platform`'s local `.env` (`TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY`) — `/account/register` now actually enforces Turnstile instead of no-oping. Real side effect caught and fixed in the same pass: 7 tests in `v2r-platform` (`test_client_account_api.py`, `test_interview_account_linkage.py`) were silently relying on the secret being blank rather than mocking it, and broke the moment a real secret existed; fixed with an autouse `conftest.py` fixture that resets `turnstile_secret_key` to `""` for every test unless a test explicitly turns it on (`v2r-platform` commit `6fd867a`). Full suite passes except one pre-existing, unrelated failure in `test_knowledge_service.py` (a hardcoded "Review status" assertion drifted out of sync with `v2r-social-media-meta`'s real current Approved status in `v2r-enterprise-knowledge`) — not touched here, flagged for whoever owns that test next. **Resolved 2026-09-12** in `v2r-platform` (`DECISIONS.md` same date): the assertion was split so the exclusion rule is tested against a synthetic Review-status row instead of a real document, since every document in `DEFAULT_KNOWLEDGE_PATHS` is now Approved. Full suite: 124 passed.
- **Queued by the founder 2026-09-07, remaining after the Explore-section hourglass workflow diagram was built:**
  1. **Wire up the discovery process end to end.** The floating entry widget's panel currently links to `mailto:contact@v2adv.com` as an honest interim channel (see README.md's "Get Started, 2026-09-06 redesign") because `v2r-platform` (registration, login, discovery form/AI interview, dashboard) has no deployment anywhere yet. Once that changes, point the widget at the real platform URLs and confirm the full path works live.
  2. **Finish setting up all social media accounts, then add social links to the header and footer.** No accounts are confirmed set up yet as of this note; neither `index.html` nor the legal pages have any social links currently.

     Real progress and a real re-sequencing since this line was first written (2026-09-08): Facebook Page (`facebook.com/V2ADV`) and an Instagram Business account (`v2adv.partners`) both exist, are connected to each other via Meta Business Suite, and have completeness fields (category, website, email, action button, bio, notification settings) done — see `project_v2adv_facebook_page.md` and `reference_facebook_page_admin_navigation.md` in Claude's memory system for the exact work and platform-navigation notes, and `docs/capabilities/social-media-capability.md` / `docs/capabilities/social-media/meta.md` (both amended the same day) in `v2r-enterprise-knowledge` for the durable, governed version of the same findings. A Google Business Profile is also in progress.

     **Founder decided 2026-09-08 the business phone line (Twilio) needs to happen next, before continuing to the remaining platforms** — not after, as originally sequenced — since the number needs to exist once, then get added everywhere (Facebook, Instagram, GBP, LinkedIn, WhatsApp), rather than setting a temporary/personal number now and swapping it everywhere later. Founder's own reasoning, verbatim in spirit: doesn't make sense to add phone fields to these accounts now if the real number isn't chosen yet, even if the number isn't displayed publicly on all of them immediately.

     Real requirements the founder stated for this line, each with why it matters:
     - **Not the founder's personal number, under any circumstance.** Founder does not want to personally answer business calls and rarely/never answers calls at all ("I loathe talking on the phone").
     - **Call recording.** The technical pattern already exists and is proven live in this portfolio (ACT Life and Health, `insurance-project/docs/twilio_setup_guide.md`, the `recordingStatusCallback` webhook pattern — real setup guide to follow, not build from scratch). **Real compliance gap, not yet resolved anywhere in this knowledge base**: `docs/capabilities/cloudflare-and-twilio-infrastructure-notes.md`'s existing Twilio section covers A2P 10DLC SMS registration and TCPA-consent-for-selling-leads (`lead-generation-compliance-notes-2026-08.md`), but neither document addresses call-recording consent law (two-party/all-party-consent states like California), which is a distinct legal question for V2ADV's own outbound/inbound business calls, not just resold leads. **No call gets recorded until this is reviewed, same standing gate as the existing SMS/TCPA rule** — see `v2r-platform/PROJECT_STATUS.md`'s identical "no live external action without founder review of the specific mechanism" pattern already applied to SMS.
     - **A texting program with proper TCPA compliance.** A2P 10DLC registration is a real, separate, already-documented prerequisite before any production SMS at all (`cloudflare-and-twilio-infrastructure-notes.md`'s Twilio section: Sole Proprietor path, 3-5 business day manual review). Whether V2ADV's own client/prospect texting additionally needs the fuller PEWC consent-capture machinery `lead-generation-compliance-notes-2026-08.md` built for reselling leads (versus a lighter first-party-relationship standard) is an open question to resolve before building any texting feature, not assumed either way.
     - **A "posture" for answering the phone.** Founder wants guidance on how a business line like this should actually be answered/handled day to day, given personal aversion to phone calls, distinct from the technical Twilio setup itself.
     - **An AI gatekeeper for calls, eventually.** Explicitly a future idea ("at some point"), not part of the initial line setup. Log and revisit once the line itself exists and real call volume/patterns are known.

     **Real platform the founder is unfamiliar with and wants guided through**: WhatsApp for business. Founder has never used WhatsApp for business before and specifically wants it for building international connections, but does not know the difference between the consumer WhatsApp Business app and the API-based WhatsApp Business Platform (what Twilio typically integrates with, and the one with a real Meta business-verification process that can take real days, not a same-day setup). Guide this choice explicitly when reached, rather than assuming the answer.

     **LinkedIn confirmed in scope, founder's explicit "definitely for linked in"** — already a documented, Approved platform (`docs/capabilities/social-media/linkedin.md`) that was mistakenly left off the founder's own first-draft sequencing list; arguably a stronger fit than TikTok/X for V2ADV's actual B2B audience.

     **Updated real sequence, per founder direction 2026-09-08**: Twilio business line (number, recording/TCPA compliance groundwork, call-answering posture) → update Facebook/Instagram/GBP with the real number → WhatsApp Business (guided choice of platform) → LinkedIn → Threads (trivial — a one-tap "Join Threads" from the existing Instagram identity, no separate signup, no phone dependency, can genuinely happen anytime) → YouTube → TikTok → X (the last three have no real dependency on the Twilio number and could move earlier if priorities change without disrupting the rest of this sequence).
  3. ~~Footer logo should be the gold wordmark image, not plain text.~~ **Resolved 2026-09-07.** All four pages (`index.html`, `privacy.html`, `terms.html`, `cookies.html`) now render the footer brand as `<img src="assets/v2adv-wordmark-gold.png?v=3">` (same asset as the header) instead of `<strong>V2ADV</strong>` text. Also dropped "Intelligent Business Systems" from the footer tagline per founder direction — footer now reads just "Vision Engineered Into Reality."
- **Future idea, explicitly out of scope for now: an 8-step numbered-badge "How V2ADV Works" showcase graphic.** Founder shared a reference image (2026-09-07) of an elaborate circular infographic — 8 numbered badges (icon + step name + one-line description) arranged around a central globe/logo, connected by arrows: Discover, Strategize, Automate, Optimize, Scale, Support, Measure, Evolve. Founder's own call: "badass, but larger than the scope we outlined with the five tenets" — this is a bigger, more elaborate showcase than the small Explore-section reinforcement graphic, better suited to a dedicated "How V2ADV Works" section or standalone page, not something to build into Explore. Reference image saved locally at `C:\Users\V2Rst\.claude\uploads\505db13c-3910-4eff-a6f2-9f810c17c8ea\a385c378-image.png` (Claude Code session upload, not guaranteed permanent — worth the founder saving a durable copy if this gets picked up later). Real open question if revisited: the 8 step names in the reference don't match V2ADV's actual established Five Disciplines (Understand, Organize, Execute, Learn, Improve) — decide whether this is meant as a separate, more detailed operational framework layered on top, or should use the real five names instead of inventing new ones.
