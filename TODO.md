# TODO — v2r-website

- **Create a Cloudflare `CF_CACHE_PURGE_TOKEN` (and note the zone's ID) for v2adv.com's zone — now urgent, not someday.** Confirmed live 2026-09-06 via direct `curl -I https://v2adv.com/...` response headers (`Server: cloudflare`, `cf-cache-status`): **v2adv.com is already running behind Cloudflare**, contradicting this file's prior assumption that the nameserver migration hadn't happened yet. Real, live consequence found the same day: Cloudflare's edge cache served a stale `globe.js` across several consecutive pushes (`Cache-Control: max-age=14400`, i.e. 4 hours, and edge nodes refreshed inconsistently — `styles.css` refreshed on its own while `globe.js` stayed stale), with no purge token configured to clear it. Worked around for now with `?v=2` cache-busting query strings on `styles.css`, `globe.js`, and `explore.js` in `index.html` — a real, working fix, but manual and requiring a version bump on every future change to those files, not a substitute for the real purge. The purge call itself is already built: `.github/workflows/cloudflare-cache-purge.yml` (added 2026-09-06) fires on every push to `master` and no-ops cleanly until `CF_ZONE_ID`/`CF_CACHE_PURGE_TOKEN` exist as this repo's GitHub Actions secrets. Founder should log into the Cloudflare dashboard directly to confirm this zone and grab both values: the v2adv.com zone → Overview → Zone ID; My Profile → API Tokens → "Cache Purge" template scoped to that zone → the token; this repo's Settings → Secrets and variables → Actions → add both. Once set, the `?v=` bumps can stop being necessary for routine changes. Full context: `docs/capabilities/cloudflare-and-twilio-infrastructure-notes.md`'s "Cloudflare Cache — Two Layers, Both Must Be Purged" section in `v2r-enterprise-knowledge`.
- **Generate a Cloudflare Turnstile site key + secret key for v2adv.com** (2026-09-06). Given the zone already exists (see above, confirmed 2026-09-06 — this may be further along than previously tracked; worth the founder checking directly), this may just be adding a new site entry rather than a first-time setup. "Managed" widget mode, matching ACT Life and Health's real configuration. `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` go into `v2r-platform`'s `.env` (see that repo's `.env.example`) — the client-account registration gate at `/account/register` no-ops without them (registration works, just unprotected by Turnstile) until they're set.
