# TODO — v2r-website

- **Create a Cloudflare `CF_CACHE_PURGE_TOKEN` for v2adv.com's zone.** Founder said "I will do this later" (2026-09-05). Cloudflare dashboard → Account → API Tokens → "Cache Purge" template, scoped to the v2adv.com zone. Once it exists, wire up a one-call purge (`POST https://api.cloudflare.com/client/v4/zones/{zoneId}/purge_cache`, body `{"purge_everything": true}`) to run after any deploy that overwrites an existing asset in place, rather than relying solely on per-file `?v=` query-string bumps. Full context: `docs/capabilities/cloudflare-and-twilio-infrastructure-notes.md`'s "Cloudflare Cache — Two Layers, Both Must Be Purged" section in `v2r-enterprise-knowledge`.
- **Generate a Cloudflare Turnstile site key + secret key for v2adv.com** (2026-09-06). Same Cloudflare account already used across the WordPress portfolio; add a new site entry for v2adv.com, "Managed" widget mode, matching ACT Life and Health's real configuration. `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` go into `v2r-platform`'s `.env` (see that repo's `.env.example`) — the client-account registration gate at `/account/register` no-ops without them (registration works, just unprotected by Turnstile) until they're set.
- **Reword the "Why V2ADV Exists" heading, `index.html:57`** (founder flagged 2026-09-06): "The real challenge is the distance between vision and reality." reads as missing a verb — founder's own instinct was something like "closing" the distance, tying it to the very next sentence ("V2ADV closes that distance through intelligent systems..."). Candidates to choose from: "The real challenge is closing the distance between vision and reality." / "The real challenge is closing the gap between vision and reality." / "What stands between vision and reality is the real challenge." Founder is open to other wording too — not decided yet.
